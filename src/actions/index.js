import * as types from "./../constants/ActionTypes";

export const addSquare = (SquareId) => ({
  type: types.
})

export const changeLevelId = (newlevelId) => ({
  type: types.CHANGE_LEVELID,
  newLevelId
});